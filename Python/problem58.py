# Given a sorted rotated array, find the index of an element in faster than linear time

def find(arr, k, start, end):
    
    if start == end:
        return
        
    return 0

assert find([13, 18, 25, 2, 8, 10], 8) == 4